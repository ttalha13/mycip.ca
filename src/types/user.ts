export interface User {
  name: string;
  email: string;
  dateOfBirth: string;
}

export interface ContactForm {
  name: string;
  email: string;
  province: string;
  phoneNumber: string;
  message: string;
}